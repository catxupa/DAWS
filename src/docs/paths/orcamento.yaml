
#paths/orcamento.yaml

paths:
    #--buscar todos os orcamentos
    /orcamento:
        get:
            tags:
                - Orcamento *
            summary: Buscar todos os orcamentos
            responses:
                "200":
                    description: Orcamentos encontrados com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "500":
                    description: Erro ao buscar orcamentos
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--criar orcamento
    /orcamento/create:
        post:
            tags:
                - Orcamento *
            summary: Criar um novo orcamento
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: "#/components/schemas/createorcamento"
            responses:
                "200":
                    description: Orcamento criado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "400":
                    description: Dados invalidos
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao criar orcamento
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--buscar orcamento por id
    /orcamento/get-by-id/{id}:
        get:
            tags:
                - Orcamento *
            summary: Buscar um orcamento por id
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                    example: "1"
            responses:
                "200":
                    description: Orcamento encontrado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "404":
                    description: Orcamento nao encontrado
                    content:
                        application/json:
                            example:
                                message: "Orcamento nao encontrado"
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao buscar orcamento
                    content:
                        application/json:
                            example:
                                message: "Erro ao buscar orcamento"
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--atualizar orcamento por id
    /orcamento/update/{id}:
        put:
            tags:
                - Orcamento *
            summary: Atualizar um orcamento por id
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                    example: "1"
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: "#/components/schemas/updateorcamento"
            responses:
                "200":
                    description: Orcamento atualizado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "400":
                    description: Dados invalidos
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao atualizar orcamento
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--deletar orcamento por id
    /orcamento/delete/{id}:
        delete:
            tags:
                - Orcamento *
            summary: Deletar um orcamento por id
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                    example: "1"
            responses:
                "200":
                    description: Orcamento deletado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "404":
                    description: Orcamento nao encontrado
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao deletar orcamento
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--calcular orcamento por id
    /orcamento/calcular/{id}:
        put:
            tags:
                - Orcamento *
            summary: Calcular um orcamento por id
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                    example: "1"
            responses:
                "200":
                    description: Orcamento calculado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "404":
                    description: Orcamento nao encontrado
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao calcular orcamento
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"


    #--buscar orcamento por id
    /orcamento/get-orcamento-por-id/{id}:
        get:
            tags:
                - Orcamento *
            summary: Buscar um orcamento por id
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: string
                    example: "10"
            responses:
                "200":
                    description: Orcamento encontrado com sucesso
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamento"
                "404":
                    description: Orcamento nao encontrado
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"
                "500":
                    description: Erro ao buscar orcamento
                    content:
                        application/json:
                            schema:
                                $ref: "#/components/schemas/orcamentoResponseError"